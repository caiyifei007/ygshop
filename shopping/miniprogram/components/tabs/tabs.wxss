.tabs_title {
    display: flex;
}

.title_item {
    display: flex;
    justify-content: center;
    align-items: center;
    flex: 1;
    padding: 15rpx 0rpx;
}

.active {
    color: var(--themeColor);
    border-bottom: 5rpx solid currentColor;
}